<UserControl x:Class="RGBSyncPlus.UI.Tabs.NewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RGBSyncPlus.UI.Tabs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="NewsItemTemplate">
            <Grid  MaxWidth="{Binding Path=ActualWidth, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListView}}, Mode=OneWay}">
                <Grid Margin="20">
                <Rectangle Fill="#808080" Opacity="0.7"></Rectangle>
                <StackPanel MaxHeight="300" Margin="20">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe UI" Text="{Binding Title}" FontSize="36"  Foreground="{DynamicResource SystemBaseHighColorBrush}"></TextBlock>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe UI" Text="{Binding Author}"  Foreground="{DynamicResource SystemBaseHighColorBrush}" FontSize="14" Margin="0,0,20,0"></TextBlock>
                        <TextBlock FontFamily="Segoe UI" Text="{Binding Date}"  Foreground="{DynamicResource SystemBaseHighColorBrush}" FontSize="14" Margin="0,0,20,0"></TextBlock>
                    </StackPanel>
                    <TextBlock FontFamily="Segoe UI Light" FontWeight="Light" Text="{Binding Content}" FontSize="16" Margin="0,10,20,0" Foreground="{DynamicResource SystemBaseHighColorBrush}" TextWrapping="WrapWithOverflow"></TextBlock>
                </StackPanel>
                </Grid>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <UserControl.DataContext>
        <local:NewsViewModel/>
    </UserControl.DataContext>
    <Grid>
        <ListView ItemsSource="{Binding NewsItems}" ItemTemplate="{DynamicResource NewsItemTemplate}"></ListView>
    </Grid>
</UserControl>
